## Algorithm Explanation ##

Two algorithms were developed. The first used the angle and distance difference between two lasers to find the orientation of the car relative to the wall. The formulae used were

$ \alpha = tan^-1 \biggl( \frac{a sin(\theta)-b}{a cos(\theta)} \biggl) $  
$ CD = bcos(\alpha) $